@extends('admin.layout')

@section('admin-content')
    <div><h1 class="text-2xl font-bold">Dashboard</h1><p class="mt-1 text-sm text-slate-600">Manage your calling platform from a fast Blade admin panel.</p></div>
    <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach ([['Clients', $stats['clients']], ['Resellers', $stats['resellers']], ['Total Calls', $stats['calls']], ['Active Servers', $stats['servers']]] as [$label, $value])<div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="text-sm text-slate-500">{{ $label }}</div><div class="mt-2 text-3xl font-bold">{{ number_format($value) }}</div></div>@endforeach</div>
    <div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-5 py-4"><h2 class="font-semibold">Recent Calls</h2></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Destination</th><th class="px-5 py-3">Client</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Created</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($recentCalls as $call)<tr><td class="px-5 py-4">{{ $call->phone_number }}</td><td class="px-5 py-4">{{ $call->user?->name }}</td><td class="px-5 py-4">{{ $call->status?->value ?? $call->status }}</td><td class="px-5 py-4 text-slate-500">{{ $call->created_at?->diffForHumans() }}</td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-slate-500">No call records yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
