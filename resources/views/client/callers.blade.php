@extends('client.layout')

@section('client-content')
    <div><h1 class="text-2xl font-bold">My SIP Caller Profiles</h1><p class="mt-1 text-sm text-slate-600">Your SIP password is encrypted and is never displayed.</p></div>
    <div class="mt-6 space-y-5">@forelse ($callers as $caller)<form method="POST" action="{{ route('client.callers.update', $caller) }}" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @method('PUT')<div class="grid gap-4 sm:grid-cols-2"><label class="text-sm font-medium">Caller name<input name="caller_name" value="{{ $caller->caller_name }}" required class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">Caller number<input name="caller_number" value="{{ $caller->caller_number }}" required class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">SIP username<input name="sip_username" value="{{ $caller->sip_username }}" required class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">New SIP password<input name="sip_password" type="password" placeholder="Leave blank to keep current password" class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">SIP domain<input name="sip_domain" value="{{ $caller->sip_domain }}" required class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">SIP port<input name="sip_port" type="number" value="{{ $caller->sip_port ?? 5060 }}" required class="mt-1 w-full rounded-lg border-slate-300"></label><label class="text-sm font-medium">SIP context<input name="sip_context" value="{{ $caller->sip_context }}" class="mt-1 w-full rounded-lg border-slate-300"></label></div><button class="mt-5 rounded-lg bg-blue-600 px-5 py-2.5 text-sm font-semibold text-white hover:bg-blue-700">Save Profile</button></form>@empty<div class="rounded-xl border border-slate-200 bg-white p-8 text-center text-slate-500">No SIP caller profiles assigned to this client.</div>@endforelse</div>
@endsection
