@extends('layouts.app')

@section('content')
    <div class="min-h-screen bg-slate-50"><header class="border-b border-slate-200 bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8"><a href="{{ route('client.dashboard') }}" class="text-lg font-bold">{{ $user->company_name ?? $user->name }}</a><nav class="flex items-center gap-4 text-sm"><a href="{{ route('client.calls') }}" class="text-slate-600 hover:text-slate-950">Calls</a><a href="{{ route('client.campaigns') }}" class="text-slate-600 hover:text-slate-950">Campaigns</a><a href="{{ route('client.callers') }}" class="text-slate-600 hover:text-slate-950">SIP Profile</a><form method="POST" action="{{ url('/logout') }}">@csrf<button class="rounded-lg border border-slate-200 px-3 py-2 hover:bg-slate-50">Logout</button></form></nav></div></header><main class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">@if (session('success'))<div class="mb-5 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @if ($errors->any())<div class="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">{{ $errors->first() }}</div>@endif @yield('client-content')</main></div>
@endsection
