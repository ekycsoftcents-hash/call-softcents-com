@extends('client.layout')

@section('client-content')
    <div><h1 class="text-2xl font-bold">Client Dashboard</h1><p class="mt-1 text-sm text-slate-600">Your calling account and FusionPBX profile overview.</p></div>
    <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="text-sm text-slate-500">Balance</div><div class="mt-2 text-3xl font-bold">{{ number_format((float) $user->balance, 2) }}</div></div><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="text-sm text-slate-500">Total Calls</div><div class="mt-2 text-3xl font-bold">{{ $user->calls()->count() }}</div></div><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="text-sm text-slate-500">Campaigns</div><div class="mt-2 text-3xl font-bold">{{ $user->campaigns()->count() }}</div></div><div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="text-sm text-slate-500">Caller Profiles</div><div class="mt-2 text-3xl font-bold">{{ $callers->count() }}</div></div></div>
    <div class="mt-6 grid gap-6 lg:grid-cols-2"><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-5 py-4"><h2 class="font-semibold">Caller Profiles</h2></div><div class="divide-y divide-slate-100">@forelse ($callers as $caller)<div class="flex items-center justify-between px-5 py-4"><div><div class="font-medium">{{ $caller->caller_name }}</div><div class="text-sm text-slate-500">{{ $caller->caller_number }} · {{ $caller->sip_username }}</div></div><span class="rounded-full px-2.5 py-1 text-xs {{ $caller->enabled ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $caller->enabled ? 'Enabled' : 'Disabled' }}</span></div>@empty<div class="px-5 py-8 text-center text-slate-500">No caller profile configured.</div>@endforelse</div></section><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-5 py-4"><h2 class="font-semibold">Recent Calls</h2></div><div class="divide-y divide-slate-100">@forelse ($recentCalls as $call)<div class="flex items-center justify-between px-5 py-4"><div><div class="font-medium">{{ $call->phone_number }}</div><div class="text-sm text-slate-500">{{ $call->created_at?->diffForHumans() }}</div></div><span class="text-sm">{{ $call->status?->value ?? $call->status }}</span></div>@empty<div class="px-5 py-8 text-center text-slate-500">No calls yet.</div>@endforelse</div></section></div>
@endsection
